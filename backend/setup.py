from setuptools import setup, find_packages

setup(
    name="book-backend",
    version="0.1.0",
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, or read from requirements.txt
    ],
)