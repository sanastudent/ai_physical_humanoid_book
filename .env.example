# AI-Driven Book + RAG Chatbot Configuration
# Copy this file to .env and fill in your actual values

# Book Configuration
BOOK_TITLE="Embodied AI: Building Intelligent Robotic Systems"

# Google API Configuration (Required for RAG chatbot with Gemini)
GOOGLE_API_KEY=your-google-api-key-here

# OpenAI Configuration (Optional - for OpenAI-based features)
OPENAI_KEY=your-openai-api-key-here

# Claude API Configuration (Required for book generation and personalization)
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Qdrant Configuration
# Use QDRANT_URL for cloud instances with full URL including protocol
QDRANT_URL=https://your-qdrant-instance.cloud.qdrant.io
QDRANT_API_KEY=your-qdrant-api-key-here

# For local instances, use QDRANT_HOST and QDRANT_PORT instead:
# QDRANT_HOST=localhost
# QDRANT_PORT=6333
QDRANT_COLLECTION_NAME=my_1st_ai_book
QDRANT_VECTOR_SIZE=1536

# Neon DB Configuration (for BetterAuth user authentication)
# Get your connection string from: https://console.neon.tech
# Format: postgresql://user:password@host/database?sslmode=require
NEON_DB_URL=postgresql://user:password@host/database?sslmode=require

# BetterAuth Configuration
# Generate a random secret for session encryption (min 32 characters)
# You can use: python -c "import secrets; print(secrets.token_urlsafe(32))"
BETTERAUTH_SECRET=generate-a-random-secret-here-min-32-chars

# FastAPI Configuration
BACKEND_HOST=0.0.0.0
BACKEND_PORT=8000

# Frontend Configuration (for API calls from React/Docusaurus)
# For local development
REACT_APP_BACKEND_URL=http://localhost:8000
# For production Railway deployment:
# REACT_APP_BACKEND_URL=https://your-app.up.railway.app
# For production Vercel backend deployment:
# REACT_APP_BACKEND_URL=https://your-backend.vercel.app

# GitHub Pages Configuration (Optional)
GITHUB_USERNAME=your-github-username
GITHUB_REPO_NAME=your-repo-name
